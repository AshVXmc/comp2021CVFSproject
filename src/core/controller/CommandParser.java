package core.controller;

public class CommandParser {
    public static void parseCommand(String command, CommandsList commandType) {
        String[] commandElements = command.split(" ");

        switch (commandType) {
            case newDisk:


        }
    }

    public static void getCommandType(){

    }
}
